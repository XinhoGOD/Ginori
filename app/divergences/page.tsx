import { MarketList } from '../../components/Views';
export default function Page() { return <MarketList kind="divergences" title="Divergencias" subtitle="Casos donde el movimiento del mercado de draft y la actividad reportada de waivers no están alineados." />; }
